# Rejected geography — HUD study only

`field-atlas.png` is a generated interface concept. The user rejected its inaccurate continent and place positions. It must **never** be used as a map, terrain source or architectural authority.

The implemented atlas follows published reference cartography instead. See [the v2 reference ledger](../../research/v2-references.md). Only the concept’s dark/brass palette and compact control arrangement informed the interface.
